name: Prepare Adaptive Prefix Recovery
on:
  push:
    branches: [adaptive-prefix-recovery]
permissions:
  contents: write
jobs:
  prepare:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'
      - name: Apply adaptive recovery update
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          def replace_once(text, old, new, label):
              if text.count(old) != 1:
                  raise SystemExit(f'{label}: expected one match, found {text.count(old)}')
              return text.replace(old, new, 1)

          prefix_logic = r'''package com.blackapps.follow;

import java.util.*;

/** Pure rules for completing a relationship list with bounded username-prefix searches. */
public final class PrefixSearchLogic {
    public static final String ALPHABET="abcdefghijklmnopqrstuvwxyz0123456789._";
    public static final int SPLIT_AT=40;
    public static final int ROWS_AT=40;
    public static final int MAX_DEPTH=4;
    public static final int MAX_QUERIES=220;
    public static final int MAX_TARGETED=64;

    private PrefixSearchLogic() {}

    public static List<String> roots() {
        ArrayList<String> result=new ArrayList<>(ALPHABET.length());
        for(int i=0;i<ALPHABET.length();i++)result.add(String.valueOf(ALPHABET.charAt(i)));
        return result;
    }

    public static List<String> children(String prefix) {
        if(prefix==null || prefix.length()>=MAX_DEPTH)return Collections.emptyList();
        ArrayList<String> result=new ArrayList<>(ALPHABET.length());
        for(int i=0;i<ALPHABET.length();i++)result.add(prefix+ALPHABET.charAt(i));
        return result;
    }

    /** Dense, already observed username groups are queried first; zero-population groups remain as fallbacks. */
    public static List<String> prioritizedChildren(String prefix,Collection<String> usernames) {
        ArrayList<String> result=new ArrayList<>(children(prefix));
        HashMap<String,Integer> counts=new HashMap<>();
        String normalized=prefix==null?"":prefix.toLowerCase(Locale.ROOT);
        if(usernames!=null)for(String username:usernames) {
            if(username==null)continue;
            String lower=username.toLowerCase(Locale.ROOT);
            if(!lower.startsWith(normalized) || lower.length()<=normalized.length())continue;
            char next=lower.charAt(normalized.length());
            if(ALPHABET.indexOf(next)<0)continue;
            String child=normalized+next;
            counts.put(child,counts.getOrDefault(child,0)+1);
        }
        result.sort((a,b)->{
            int byCount=Integer.compare(counts.getOrDefault(b,0),counts.getOrDefault(a,0));
            if(byCount!=0)return byCount;
            return Integer.compare(ALPHABET.indexOf(a.charAt(a.length()-1)),ALPHABET.indexOf(b.charAt(b.length()-1)));
        });
        return result;
    }

    public static int population(String prefix,Collection<String> usernames) {
        int count=0;if(prefix==null||usernames==null)return 0;
        for(String username:usernames)if(matches(username,prefix))count++;
        return count;
    }

    public static boolean matches(String username,String prefix) {
        if(username==null || prefix==null || prefix.isEmpty())return false;
        return username.toLowerCase(Locale.ROOT).startsWith(prefix.toLowerCase(Locale.ROOT));
    }

    public static int targetedLimit(int missing) {
        if(missing<=0)return 0;
        return Math.min(MAX_TARGETED,Math.max(12,missing*4));
    }

    public static boolean shouldSplit(String prefix,int matchingRows,int totalRows,boolean incomplete) {
        return prefix!=null && prefix.length()<MAX_DEPTH &&
            (incomplete || matchingRows>=SPLIT_AT || (matchingRows>0 && totalRows>=ROWS_AT));
    }
}
'''
          Path('app/src/main/java/com/blackapps/follow/PrefixSearchLogic.java').write_text(prefix_logic,encoding='utf-8')

          prefix_test = r'''import com.blackapps.follow.PrefixSearchLogic;
import java.util.*;

public class PrefixSearchLogicTest {
    static int checks;
    static void check(boolean ok,String name){checks++;if(!ok)throw new AssertionError(name);}
    public static void main(String[] args){
        List<String> roots=PrefixSearchLogic.roots();
        check(roots.size()==38,"all legal Instagram username starters covered");
        check(roots.get(0).equals("a")&&roots.contains("0")&&roots.contains(".")&&roots.contains("_"),"letters digits and punctuation covered");
        check(new HashSet<>(roots).size()==roots.size(),"root prefixes unique");
        check(PrefixSearchLogic.matches("Abc.Def","ab"),"prefix match is case insensitive");
        check(!PrefixSearchLogic.matches("name_ab","ab"),"substring-only search result rejected");
        check(PrefixSearchLogic.shouldSplit("a",40,40,false),"dense matching result splits");
        check(PrefixSearchLogic.shouldSplit("a",1,1,true),"server-limited result splits");
        check(!PrefixSearchLogic.shouldSplit("a",0,50,false),"full-name noise alone does not explode the queue");
        check(!PrefixSearchLogic.shouldSplit("abcd",100,100,true),"bounded depth prevents unbounded requests");
        List<String> names=Arrays.asList("ab1","ab2","ab3","ac1","a9x","other");
        List<String> children=PrefixSearchLogic.prioritizedChildren("a",names);
        check(children.size()==38&&children.get(0).equals("ab"),"densest observed child is first");
        check(children.get(1).equals("ac")&&children.get(2).equals("a9"),"equal populations retain alphabet priority");
        check(PrefixSearchLogic.population("ab",names)==3,"known prefix population counted");
        check(PrefixSearchLogic.children("abcd").isEmpty(),"maximum depth enforced");
        check(PrefixSearchLogic.targetedLimit(1)==12,"small gaps receive a useful targeted allowance");
        check(PrefixSearchLogic.targetedLimit(6)==24,"target allowance scales with the missing count");
        check(PrefixSearchLogic.targetedLimit(100)==PrefixSearchLogic.MAX_TARGETED,"target allowance is capped");
        check(PrefixSearchLogic.MAX_QUERIES>=roots.size()+PrefixSearchLogic.MAX_TARGETED,"budget covers targets and every root");
        System.out.println("PASS: "+checks+" adaptive prefix-search checks");
    }
}
'''
          Path('tests/PrefixSearchLogicTest.java').write_text(prefix_test,encoding='utf-8')

          store_path=Path('app/src/main/java/com/blackapps/follow/Store.java')
          store=store_path.read_text(encoding='utf-8')
          previous_block='''    private LinkedHashMap<String,Edge> previous(long account,String kind) {
        LinkedHashMap<String,Edge> out=new LinkedHashMap<>();
        try(Cursor c=getReadableDatabase().rawQuery("SELECT person,username,name,since,lower_bound,avatar FROM edges WHERE account=? AND kind=?",new String[]{""+account,kind})) {
            while(c.moveToNext()) { Edge e=new Edge(c.getString(0),c.getString(1),c.getString(2));e.since=c.getLong(3);e.lower=c.getLong(4);e.avatar=c.getString(5);out.put(e.id,e); }
        }
        return out;
    }
'''
          baseline_block=previous_block+'''    /** Last fully verified identities, used only to target recovery queries after a short scan. */
    public LinkedHashMap<String,Edge> baseline(long account,String owner,String kind) {
        if(!(kind.equals("followers")||kind.equals("following")))throw new IllegalArgumentException("Geçersiz liste türü.");
        LinkedHashMap<String,Edge> out=new LinkedHashMap<>();
        try(Cursor c=getReadableDatabase().rawQuery("SELECT e.person,e.username,e.name,e.since,e.lower_bound,e.avatar FROM edges e JOIN accounts a ON a.id=e.account WHERE e.account=? AND a.owner=? AND e.kind=?",new String[]{""+account,owner,kind})) {
            while(c.moveToNext()) { Edge e=new Edge(c.getString(0),c.getString(1),c.getString(2));e.since=c.getLong(3);e.lower=c.getLong(4);e.avatar=c.getString(5);out.put(e.id,e); }
        }
        return out;
    }
'''
          store=replace_once(store,previous_block,baseline_block,'Store baseline insertion')
          store_path.write_text(store,encoding='utf-8')

          client_path=Path('app/src/main/java/com/blackapps/follow/InstagramClient.java')
          client=client_path.read_text(encoding='utf-8')
          trace_search='''    private void traceSearch(String kind,int requests,int rows,int unique,int expected,int queued,int depth,boolean split) {
        listTrace.put("Search"+kind,"Önek araması "+("followers".equals(kind)?"takipçi":"takip")+": istek="+requests+", satır="+rows+", kişi="+unique+"/"+expected+", kuyruk="+queued+", derinlik="+depth+", bölündü="+split);
        saveListTrace();
    }
'''
          trace_both=trace_search+'''    private void traceTarget(String kind,int requests,int candidates,int recovered,int unique,int expected) {
        listTrace.put("Target"+kind,"Önceki listeden hedefli arama "+("followers".equals(kind)?"takipçi":"takip")+": istek="+requests+", aday="+candidates+", bulunan="+recovered+", kişi="+unique+"/"+expected);
        saveListTrace();
    }
'''
          client=replace_once(client,trace_search,trace_both,'target trace insertion')

          new_search_block=r'''    private static final class PrefixResult {int requests,rows,matches;boolean incomplete;}
    private static final class PrefixTask {
        final String prefix;final int score;
        PrefixTask(String prefix,int score){this.prefix=prefix;this.score=score;}
    }
    private static final Comparator<PrefixTask> PREFIX_ORDER=(left,right)->{
        int byScore=Integer.compare(right.score,left.score);if(byScore!=0)return byScore;
        int byDepth=Integer.compare(right.prefix.length(),left.prefix.length());if(byDepth!=0)return byDepth;
        return left.prefix.compareTo(right.prefix);
    };
    private static List<String> usernames(LinkedHashMap<String,Store.Edge> found) {
        ArrayList<String> out=new ArrayList<>(found.size());for(Store.Edge edge:found.values())out.add(edge.username);return out;
    }
    private void scheduleChildren(PriorityQueue<PrefixTask> queue,Set<String> scheduled,String parent,LinkedHashMap<String,Store.Edge> found) {
        List<String> names=usernames(found);
        for(String child:PrefixSearchLogic.prioritizedChildren(parent,names))
            if(scheduled.add(child))queue.add(new PrefixTask(child,PrefixSearchLogic.population(child,names)));
    }
    private PrefixResult searchPrefix(String id,String kind,String prefix,String rankToken,LinkedHashMap<String,Store.Edge> found,int budget) throws Exception {
        PrefixResult result=new PrefixResult();String cursor="";HashSet<String> cursors=new HashSet<>(),matched=new HashSet<>();
        for(int page=0;page<20 && result.requests<budget;page++) {
            JSONObject j=get(listPath(id,kind,prefix,rankToken,cursor));result.requests++;
            JSONArray users=j.getJSONArray("users");result.rows+=users.length();
            for(int i=0;i<users.length();i++) {
                Store.Edge person=edge(users.getJSONObject(i));
                if(PrefixSearchLogic.matches(person.username,prefix)){matched.add(person.id);found.put(person.id,person);}
            }
            if(found.size()>1000000)throw new IOException("Liste güvenli işleme sınırını aştı; geçmiş korunuyor.");
            String next=j.isNull("next_max_id")?"":j.optString("next_max_id","");
            boolean more=j.optBoolean("has_more",false)||!next.isEmpty();
            result.incomplete|=j.optBoolean("should_limit_list_of_followers",false);
            if(!more){result.matches=matched.size();return result;}
            if(users.length()==0||next.isEmpty()||!cursors.add(next)){result.incomplete=true;result.matches=matched.size();return result;}
            cursor=next;
        }
        result.incomplete=true;result.matches=matched.size();return result;
    }
    private int recoverBaseline(String id,String kind,int expected,String rankToken,LinkedHashMap<String,Store.Edge> baseline,LinkedHashMap<String,Store.Edge> found,int budget) throws Exception {
        if(baseline==null||baseline.isEmpty()||found.size()>=expected||budget<=0)return 0;
        ArrayList<Store.Edge> candidates=new ArrayList<>();
        for(Store.Edge old:baseline.values())if(!found.containsKey(old.id))candidates.add(old);
        candidates.sort((a,b)->a.username.compareToIgnoreCase(b.username));
        int limit=Math.min(candidates.size(),Math.min(budget,PrefixSearchLogic.targetedLimit(expected-found.size())));
        int requests=0,recovered=0;
        for(int i=0;i<limit&&found.size()<expected;i++) {
            guard();Store.Edge candidate=candidates.get(i);
            JSONObject j=get(listPath(id,kind,candidate.username,rankToken,""));requests++;
            JSONArray users=j.getJSONArray("users");
            for(int n=0;n<users.length();n++) {
                Store.Edge person=edge(users.getJSONObject(n));
                if(person.id.equals(candidate.id)||person.username.equalsIgnoreCase(candidate.username)) {
                    if(!found.containsKey(person.id))recovered++;
                    found.put(person.id,person);
                }
            }
            if(found.size()>expected)throw new IOException("Liste kontrol sırasında değişti veya hedefli arama beklenmeyen kişi döndürdü; geçmiş korunuyor. [BF_LIST_CHANGED]");
            traceTarget(kind,requests,limit,recovered,found.size(),expected);
            progress.page(kind+"_target",requests,found.size(),expected);
        }
        return requests;
    }
    private LinkedHashMap<String,Store.Edge> completeBySearch(String id,String kind,int expected,String rankToken,LinkedHashMap<String,Store.Edge> baseline,LinkedHashMap<String,Store.Edge> found) throws Exception {
        if(found.size()==expected)return found;
        int requests=0,rows=0;
        PriorityQueue<PrefixTask> queue=new PriorityQueue<>(PREFIX_ORDER);HashSet<String> scheduled=new HashSet<>();
        try {
            requests+=recoverBaseline(id,kind,expected,rankToken,baseline,found,PrefixSearchLogic.MAX_QUERIES-requests);
            if(found.size()==expected){guard();observer.received(kind,found,expected);return found;}
            for(String prefix:PrefixSearchLogic.roots()) {
                if(found.size()>=expected||requests>=PrefixSearchLogic.MAX_QUERIES)break;
                guard();PrefixResult part=searchPrefix(id,kind,prefix,rankToken,found,PrefixSearchLogic.MAX_QUERIES-requests);
                requests+=part.requests;rows+=part.rows;
                if(found.size()>expected)throw new IOException("Liste kontrol sırasında değişti veya arama beklenmeyen kişi döndürdü; geçmiş korunuyor. [BF_LIST_CHANGED]");
                boolean split=PrefixSearchLogic.shouldSplit(prefix,part.matches,part.rows,part.incomplete);
                if(split)scheduleChildren(queue,scheduled,prefix,found);
                traceSearch(kind,requests,rows,found.size(),expected,queue.size(),prefix.length(),split);
                progress.page(kind+"_search",requests,found.size(),expected);
            }
            while(!queue.isEmpty()&&found.size()<expected&&requests<PrefixSearchLogic.MAX_QUERIES) {
                guard();PrefixTask task=queue.poll();
                PrefixResult part=searchPrefix(id,kind,task.prefix,rankToken,found,PrefixSearchLogic.MAX_QUERIES-requests);
                requests+=part.requests;rows+=part.rows;
                if(found.size()>expected)throw new IOException("Liste kontrol sırasında değişti veya arama beklenmeyen kişi döndürdü; geçmiş korunuyor. [BF_LIST_CHANGED]");
                boolean split=PrefixSearchLogic.shouldSplit(task.prefix,part.matches,part.rows,part.incomplete);
                if(split)scheduleChildren(queue,scheduled,task.prefix,found);
                traceSearch(kind,requests,rows,found.size(),expected,queue.size(),task.prefix.length(),split);
                progress.page(kind+"_search",requests,found.size(),expected);
            }
            guard();observer.received(kind,found,expected);return found;
        } catch(Exception e) {
            observer.received(kind,found,expected);throw e;
        }
    }
'''
          pattern=r'    private static final class PrefixResult \{.*?\n    public Profile readProfile'
          replacement=new_search_block+'    public Profile readProfile'
          client,new_count=re.subn(pattern,replacement,client,count=1,flags=re.S)
          if new_count!=1:
              raise SystemExit(f'Instagram search block: expected one match, found {new_count}')

          old_snapshot='''        String followerRank=owner+"_"+UUID.randomUUID().toString(),followingRank=owner+"_"+UUID.randomUUID().toString();
        s.followers=people(s.profile.id,"followers",s.profile.followers,followerRank);
        s.following=people(s.profile.id,"following",s.profile.following,followingRank);
        s.followers=completeBySearch(s.profile.id,"followers",s.profile.followers,followerRank,s.followers);
        s.following=completeBySearch(s.profile.id,"following",s.profile.following,followingRank,s.following);
'''
          new_snapshot='''        String followerRank=owner+"_"+UUID.randomUUID().toString(),followingRank=owner+"_"+UUID.randomUUID().toString();
        LinkedHashMap<String,Store.Edge> oldFollowers=new LinkedHashMap<>(),oldFollowing=new LinkedHashMap<>();
        try(Store history=new Store(context)) {
            oldFollowers=history.baseline(a.id,a.owner,"followers");
            oldFollowing=history.baseline(a.id,a.owner,"following");
        } catch(Exception ignored) { /* Baseline recovery is an optimization; the live scan remains authoritative. */ }
        s.followers=people(s.profile.id,"followers",s.profile.followers,followerRank);
        s.following=people(s.profile.id,"following",s.profile.following,followingRank);
        s.followers=completeBySearch(s.profile.id,"followers",s.profile.followers,followerRank,oldFollowers,s.followers);
        s.following=completeBySearch(s.profile.id,"following",s.profile.following,followingRank,oldFollowing,s.following);
'''
          client=replace_once(client,old_snapshot,new_snapshot,'snapshot baseline wiring')
          client_path.write_text(client,encoding='utf-8')

          monitor_path=Path('app/src/main/java/com/blackapps/follow/Monitor.java')
          monitor=monitor_path.read_text(encoding='utf-8')
          monitor=replace_once(monitor,'            long deadline=System.currentTimeMillis()+7*60000;\n','            long deadline=System.currentTimeMillis()+(force&&!profileOnly?12:7)*60000;\n','manual deadline')
          old_progress='''            InstagramClient client=new InstagramClient(c,owner,deadline,(kind,page,received,total)->progress=(kind.startsWith("followers")?"Takipçiler":"Takip edilenler")+(kind.endsWith("_search")?" • önek araması":"")+": "+received+"/"+total+" kişi • sayfa "+page);
'''
          new_progress='''            InstagramClient client=new InstagramClient(c,owner,deadline,(kind,page,received,total)->progress=(kind.startsWith("followers")?"Takipçiler":"Takip edilenler")+(kind.endsWith("_target")?" • hedefli arama":kind.endsWith("_search")?" • önek araması":"")+": "+received+"/"+total+" kişi • "+((kind.endsWith("_target")||kind.endsWith("_search"))?"istek ":"sayfa ")+page);
'''
          monitor=replace_once(monitor,old_progress,new_progress,'progress labels')
          monitor_path.write_text(monitor,encoding='utf-8')

          main_path=Path('app/src/main/java/com/blackapps/follow/MainActivity.java')
          main=main_path.read_text(encoding='utf-8').replace('0.4.1','0.4.2')
          main_path.write_text(main,encoding='utf-8')

          gradle=Path('app/build.gradle')
          value=gradle.read_text(encoding='utf-8')
          value=replace_once(value,"versionCode 16; versionName '0.4.1-test'","versionCode 17; versionName '0.4.2-test'",'Gradle version')
          gradle.write_text(value,encoding='utf-8')

          manifest=Path('app/src/main/AndroidManifest.xml')
          value=manifest.read_text(encoding='utf-8')
          value=replace_once(value,'android:versionCode="16" android:versionName="0.4.1-test"','android:versionCode="17" android:versionName="0.4.2-test"','Manifest version')
          manifest.write_text(value,encoding='utf-8')

          build=Path('build-apk.sh')
          value=build.read_text(encoding='utf-8').replace('Black-Follow-0.4.1-test.apk','Black-Follow-0.4.2-test.apk')
          build.write_text(value,encoding='utf-8')

          for name in ['README.md','TEST-RESULTS.md','GITHUB-SETUP.md']:
              path=Path(name);value=path.read_text(encoding='utf-8').replace('0.4.1','0.4.2').replace('versionCode=16','versionCode=17')
              path.write_text(value,encoding='utf-8')

          readme=Path('README.md')
          value=readme.read_text(encoding='utf-8')
          marker='Instagram takipçi ve takip listelerine oturumun izin verdiği ölçüde erişip yerel geçmiş tutan bağımsız Android uygulaması. Instagram veya Meta\'nın resmî uygulaması değildir.\n'
          section='''\n## 0.4.2: hedefli kurtarma ve yoğunluk önceliği\n\n- Normal liste eksik kaldığında, son doğrulanmış listede bulunup yeni yanıtta görünmeyen kullanıcılar önce tam kullanıcı adlarıyla hedefli aranır.\n- Geniş önek taramasında kalabalık alt önekler önce işlenir; olası olmayan boş kombinasyonlar kuyruğun başını tüketmez.\n- Sadece görünen ad eşleşmesi getiren kalabalık aramalar artık gereksiz alt dallara ayrılmaz.\n- Elle başlatılan tam kontrolün güvenli süre sınırı 12 dakikadır; otomatik kontroller 7 dakikalık sınırı korur.\n- Tam sayı yine doğrulanamazsa önizleme saklanır ve geçmiş değişmez.\n'''
          if section.strip() not in value:
              value=replace_once(value,marker,marker+section,'README section')
          readme.write_text(value,encoding='utf-8')

          results=Path('TEST-RESULTS.md')
          value=results.read_text(encoding='utf-8')
          extra='''\n## 0.4.2 ek kontrolleri\n\n- Önceki doğrulanmış listedeki eksik kullanıcılar için sınırlı hedefli arama bütçesi.\n- Yoğun alt öneklerin önceliklendirilmesi ve sıfır eşleşmeli görünen-ad gürültüsünün bölünmemesi.\n- Dört karakterlik üst derinlik, toplam 220 istek bütçesi ve elle taramada 12 dakikalık zaman sınırı.\n- Tam sonucun dışında geçmiş ve bildirim üretmeme kuralı değişmedi.\n'''
          if extra.strip() not in value:value+=extra
          results.write_text(value,encoding='utf-8')
          PY
          bash test.sh
          export ANDROID_SDK_ROOT="$ANDROID_HOME"
          bash build-apk.sh
          ! grep -R "0.4.1" app README.md TEST-RESULTS.md GITHUB-SETUP.md build-apk.sh
      - name: Commit prepared sources
        run: |
          rm .github/workflows/prepare-adaptive-prefix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Prioritize targeted and dense prefix recovery"
          git push origin HEAD:adaptive-prefix-recovery
