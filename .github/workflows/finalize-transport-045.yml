name: Superseded Transport Audit 0.4.5
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by finalize-transport-v2-045.yml'